\appendix
\chapter{Appendix}
\label{chap:appendix}

\section{Specializing the SDE for DDPM}
\label{app:sde_ddpm}
Section \ref{sec:score_based} introduced the general SDE framework. This appendix provides the specific forms of these SDEs for the DDPMs models, which are defined by a noise schedule $\beta(t)$.
\subsection{DDPM Forward SDE}
The general forward SDE is given by:

\begin{equation}
	dx=f(x,t)dt+g(t)dw
\end{equation}
For the popular Variance Preserving (VP) SDE, which is the continuous-time equivalent of the DDPM forward process, the coefficients are chosen as follows:
\begin{itemize}
    \item \textbf{Drift Coefficient:} 
    \[
    f(x,t) = -\tfrac{1}{2}\beta(t)x
    \]
    This is a linear drift term that deterministically pulls any data point $x$ towards the origin (the mean of the noise distribution). The rate at which it pulls is controlled by the noise schedule $\beta(t)$.

    \item \textbf{Diffusion Coefficient:}
    \[
    g(t) = \sqrt{\beta(t)}
    \]
    This term controls the magnitude of the random noise injected into the process at time $t$. The amount of noise is also directly governed by the noise schedule $\beta(t)$.
\end{itemize}
Substituting these specific functions into the general SDE yields the precise form for the DDPM forward process:
\begin{equation}
	dx=-\frac{1}{2}\beta(t)xdt+\sqrt{\beta(t)}dw
\end{equation}

\subsection{DDPM Reverse SDE}
The corresponding general reverse-time SDE, which transforms noise back into data, is defined as:
\begin{equation}
	d{x} = \left[f({x}, t) - g(t)^2\nabla_{\mathbf{x}} \log p_t(\mathbf{x})\right] dt + g(t)d\bar{w}
\end{equation}
\begin{equation}
	dx=[-\frac{1}{2}\beta(t)x-\beta(t)\nabla_x\log p_t(x)]dt+\sqrt{\beta(t)}d\bar{w}
\end{equation}
This is the equation that is solved during the sampling process. The score function allows the model to deterministically drift the samples towards regions of high data density while being guided by the stochastic diffusion term.

\section{Derivation of the CFM Objective}
\label{app:cfm_derivation}
The Flow Matching framework is built upon a distinction between marginal and conditional formulations. The marginal quantities represent the true flow to be learned but are computationally intractable. The conditional quantities, however, are simple and tractable. The main takeaway is that a model trained on the tractable conditional objective provably converges to the desired marginal model.

\subsection{Marginal Formulations}
The marginal formulations describe the flow of transforming the noise distribution into the entire data distribution $p_data$ by averaging over all possible data points $z$.
\begin{enumerate}
	\item \textbf{Marginal Probability Path $p_t$}: This describes the distribution of points at time $t$ as it morphs from simple noise into the full data distribution, defined by an integral over all possible conditional paths:
		\begin{equation}
			p_t(x)=\int p_t(x|z)p_{data}(z)dz.
		\end{equation}
		Because $p_{\text{data}}$ is complex, this integral is intractable.
	\item \textbf{Marginal Vector Field $u_{t}^{\text{target}}(x)$}: This is the average vector field that generates the marginal path and is the target for the neural network to learn. It is also defined by an intractable integral: 
		\begin{equation}
			u_t^{\text{target}}(x) = \int u_t^{\text{target}}(x|z) \frac{p_t(x|z)p_{\text{data}}(z)}{p_t(x)} dz
		\end{equation}
\end{enumerate}

\subsection{Conditional Formulations}
To solve the intractability of the marginal formulations, simpler conditional quantities are defined with respect to a single point  $z \sim p_{\text{data}}$. These describe the process of transforming noise into that one chosen sample and are analytically tractable.

\begin{enumerate}
	\item \textbf{Conditional Probability Path $p_t( \cdot \mid z)$}: This describes a path of probability distributions that interpolates between the initial noise distribution $p_{\text{init}}$ and a single data point $z$.
        For the Gaussian path used in CFM, the position on this path at time $t$ is defined by the noise schedulers $\alpha_t$ and $\beta_{t}$:
        \begin{equation}
        	x_t = \alpha_t z + \beta_tx_0
        \end{equation}
        The distribution at time $t$ is therefore 
        \begin{equation}
        	p_t(x|z)=\mathcal{N}(\alpha_t z, \beta_t^2 I_d)
        \end{equation}
    \item \textbf{Conditional Vector Field $ u_t^{target}(x\mid z)$}: This is the ground truth velocity field that generates the conditional probability path. Taking the derivative of the path equation with respect to time gives the velocity in terms of the start and end points:
    	\begin{equation}
    		\text{velocity} = \frac{d}{dt}(\alpha_t z + \beta_t x_0) = \dot{\alpha}_t + \dot{\beta}_tx_0
    	\end{equation}
    	but the neural network only has access to the current position on the path $x$, not the initial noise $x_0$. We can solve $x_0$ by rearranging the path equation:
    	\begin{equation}
    		x_0 = \frac{x- \alpha_t z} {\beta_t}
    	\end{equation}
    	Substituting this back into the velocity equation yields the final vector field formula in terms of the current position $x$ and the target data point $z$:
    	\begin{equation}
    		u_t^{target}(x\mid z)=\left(\frac{\dot{\alpha}_t}{\alpha_t}-\frac{\beta_t\dot{\beta}_t}{\alpha_t}\right)z+\frac{\beta_t\dot{\beta}_t}{\alpha_t}x
    	\end{equation}
    	This tractable field serves as the direct regression target for the neural network.
\end{enumerate}

\subsection{The Conditional Flow Matching Solution}
As shown in Section \ref{sec:cfm_formulation}, the general Flow Matching objective is to minimize the L2 distance between the learned model $v_\theta$ and the intractable marginal vector field $u_t^{target}(x)$. Using the “marginalization trick”, one can prove that loss is equivalent (up to a constant) to minimizing the loss with respect to the tractable conditional vector field $u_t^{target}(x \mid z)$ . 



\section{Joint Training Objective for Hierarchical Planners}
\label{app:joint_training}
Training a monolithic joint flow, despite its benefits, also introduces new potential failure modes, primarily error propagation. Poorly generated waypoints can lead the action planner astray and inconsistencies may arise if the two components are not well-aligned. These challenges can theoretically be mitigated through joint training.
As established in Section \ref{sec:framework_design}, the chain rule of probability provides a way to factorize the joint trajectory distribution. Calculating the total log-likelihood of the data is then equivalent to maximizing the sum of the log-likelihoods of the two hierarchical components:
\begin{equation}
	\log p(\tau_s, \tau_a \mid c) = \log p(\tau_a \mid \tau_s, c) + \log p(\tau_s \mid c).
\end{equation}
where $\lambda$ is a weighting hyperparameters to balance the two terms.
To further enforce consistency, this joint objective can be augmented with auxiliary losses such as a reachability penalty. This penalty uses a learned one-step dynamics model $f_\phi (s_k, a_k)$ to predict the state that would be reached if the Action Planner were to execute its plan [30]. The loss that penalizes the distance between the predicted state and the waypoint intended can be formulate as:
\begin{equation}
	\mathcal{L}_{\text{reach}}(\theta_{WP},\theta_{AP})=\mathbb{E}_{\tau_s^{\text{gen}}}\left[\sum_{k=0}^{H-1}\|f_{\phi}(s_k^{\text{gen}},a_k^{\text{gen}})-s_{k+1}^{\text{gen}}\|^2\right],
\end{equation}
where $s_{k}^{gen}$ is a waypoint generated by the WP, and $a_{k}^{gen}$ is the action generated by the AP to reach the next waypoint. Such a penalty is designed to penalized waypoint transitions that are dissimilar to state transitions covered in the expert dataset, thereby grounding the high-level plan in physically plausible behaviors.

The factorized joint training preserves end-to-end coherence through a single scalar objective while maintaining modularity and disentanglement. Specifically, the state flow can be trained on unlabeled trajectories, reused across different low-level controllers, or even swapped when transferring to new embodiment. In contrast, a flat joint flow offers maximal expressivity but no such modularity, and its latent representation entangles state and action dynamics that lacks interpretability.


\section{Latent Dataset Generation}
\label{app:latent_dataset}
The high-dimensional visual observation spaces of the CarRacing-v3 environment required a specialized data processing pipeline to make the planning problem tractable. This involved training a Variational Autoencoder (VAE) to learn a compressed latent representation of the environment’s visual states.

\subsection{VAE Training}
\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{vae_pipeline}
  \caption{VAE Pipeline}
  \label{fig:vae_pipeline}
\end{figure}


\subsection{Latent Dataset and Inference Wrapper}
Following the training of the VAE, its encoder was used to process the entire expert PPO dataset. Each image observation in the expert trajectories was passed through the trained encoder to generate a corresponding latent vector. This resulted in a new dataset containing sequences of \texttt{(latent state, action)} pairs, which was then used to train the Flow Planner models.

\subsection{VAE Training}
\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{vae_loss}
  \caption{VAE Loss Curves}
  \label{fig:vae_loss}
\end{figure}

\subsection{VAE Training}
\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{vae_results}
  \caption{VAE Reconstruction Results}
  \label{fig:vae_results}
\end{figure}

To facilitate inference, a custom Gymnasium wrapper was created. This wrapper integrates the trained VAE encoder so the planner can operate in real-time by automatically encoding the visual observations from the environment into the latent space.
